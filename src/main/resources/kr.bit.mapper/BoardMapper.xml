<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.bit.mapper.BoardMapper">
    <insert id="insertBoard" parameterType="kr.bit.entity.Board">
        <selectKey resultType="int" order="BEFORE" keyProperty="id">
            select IFNULL(max(id)+1,1) as id from boards
        </selectKey>
        insert into boards(id,writer_id,title,content)
        value(#{id},#{writer_id},#{title},#{content})
    </insert>
</mapper>
